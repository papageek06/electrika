<form method="post" action="{{ path('app_galery_picture_delete', {'id': galery_picture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ galery_picture.id) }}">
    <button class="btn">Supprimer</button>
</form>
