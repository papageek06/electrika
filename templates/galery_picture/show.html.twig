{% extends 'base.html.twig' %}

{% block title %}GaleryPicture{% endblock %}

{% block body %}
    <h2>GaleryPicture</h2>
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ galery_picture.id }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ galery_picture.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_galery_picture_index') }}">Revenir à la liste</a>

    <a href="{{ path('app_galery_picture_edit', {'id': galery_picture.id}) }}">Modifier</a>

    {{ include('galery_picture/_delete_form.html.twig') }}
{% endblock %}
