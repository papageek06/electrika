{% extends 'base.html.twig' %}

{% block title %}Absence{% endblock %}

{% block body %}
    <h1>Absence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ absence.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ absence.type }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ absence.startDate ? absence.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ absence.endDate ? absence.endDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ absence.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_absence_index') }}">Revenir à la liste</a>

    <a href="{{ path('app_absence_edit', {'id': absence.id}) }}">Modifier</a>

    {{ include('absence/_delete_form.html.twig') }}
{% endblock %}
