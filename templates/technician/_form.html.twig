{% form_theme form 'bootstrap_5_layout.html.twig' %}

<div class="MyDiv col-12 col-md-6">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn bg-warning">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

</div>
    

