{% extends 'base.html.twig' %}

{% block title %}Technician{% endblock %}

{% block body %}
    <h1>Technician</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ technician.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ technician.status }}</td>
            </tr>
            <tr>
                <th>HireDate</th>
                <td>{{ technician.hireDate ? technician.hireDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Specialities</th>
                <td>{{ technician.specialities ? technician.specialities|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_technician_index') }}">Revenir à la liste</a>

    <a href="{{ path('app_technician_edit', {'id': technician.id}) }}">Modifier</a>

    {{ include('technician/_delete_form.html.twig') }}
{% endblock %}
