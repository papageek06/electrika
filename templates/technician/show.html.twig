{% extends 'base.html.twig' %}

{% block title %}Technician
{% endblock %}

{% block body %}
	<h1 class="text-light text-center">Technician</h1>

	<section class="MyDiv">
		<table class="table table-dark">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ technician.id }}</td>
				</tr>
				<tr>
					<th>Status</th>
					<td>{{ technician.status }}</td>
				</tr>
				<tr>
					<th>HireDate</th>
					<td>{{ technician.hireDate ? technician.hireDate|date('Y-m-d') : '' }}</td>
				</tr>
				<tr>
					<th>Specialities</th>
					<td>{{ technician.specialities ? technician.specialities|join(', ') : '' }}</td>
				</tr>
			</tbody>
		</table>
	</section>

	<a class="btn bg-primary" href="{{ path('app_technician_index') }}">Revenir à la liste</a>

	<a class="btn bg-warning" href="{{ path('app_technician_edit', {'id': technician.id}) }}">Modifier</a>

	{{ include('technician/_delete_form.html.twig') }}
{% endblock %}
