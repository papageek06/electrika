{% extends 'base.html.twig' %}

{% block title %}Connector{% endblock %}

{% block body %}
    <h2>Connector</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ connector.id }}</td>
            </tr>
            <tr>
                <th>Power</th>
                <td>{{ connector.power }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ connector.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_connector_index') }}">Revenir à la liste</a>

    <a href="{{ path('app_connector_edit', {'id': connector.id}) }}">Modifier</a>

    {{ include('connector/_delete_form.html.twig') }}
{% endblock %}
