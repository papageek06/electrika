<form method="post" action="{{ path('app_product_connector_delete', {'id': product_connector.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_connector.id) }}">
    <button class="btn">Delete</button>
</form>
